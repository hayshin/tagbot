version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-tag-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - TELOXIDE_TOKEN=${TELOXIDE_TOKEN}
      - RUST_LOG=info
      - DATABASE_URL=/app/data/tagbot.db
